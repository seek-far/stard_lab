# Results

This directory contains experiment configs, evaluation metrics, and representative reranked dev rankings.

Large adapter weights, tokenizer files, model binaries, and full rerank prediction JSONL files are not included. The main result summary is in `../README.md`.
